from frappe.model.document import Document


class SXCachLam(Document):
    """Cách thức hoàn thành một mã hàng — "Làm tay", "Máy hỗ trợ"… (D67).

    Cùng một mã hàng làm bằng cách khác nhau thì đơn giá khoán khác nhau, nên đây là
    một CHIỀU của bảng đơn giá chứ không phải thuộc tính của sản phẩm.
    Tên đặt tay (autoname prompt) vì nó là từ nhà xưởng đang dùng, không phải mã."""

    pass
